M-C5-001 | skill lifecycle triage | refinement loop repairs the skills the agent depends on, tolerates regressions on unused ones, and accepts a long create-and-forget tail; per-seed skill-lane viz marks add/update/run/delete events with update-to-next-execute edges so each debug iteration reads as update followed by run | C.1.2 Skill refinement loop
M-C5-002 | sub-agent span metrics | each run segmented into orchestrator and sub-agent execution spans; approximate per-step input tokens derived from prompt length; spans labeled by task type; "exit" = percent of spans ending via return_to_orchestrator; "focus" = percent of returns where orchestrator either pursued the pre-handoff objective or crossed a milestone within ten subsequent steps | C.1.3 Sub-agent handoffs
M-C5-003 | memory catalog exposure and pull rate | every orchestrator step prompt lists IDs and titles of all stored memories under a "LONG-TERM MEMORY OVERVIEW" section (free full catalog); pull rate measured as fraction of available entries ever referenced in an episode, via process_memory full-content request, memorized-skill invocation, or entry-ID citation in reasoning; reads localized to milestone windows (Mauville Gym on Emerald, Mt Moon on Red) | C.1.4 Memory reuse
M-C5-004 | bootstrap inheritance classification | every invocation classified as bootstrap-originated vs authored-during-run using retrieval semantics that count actual use across stores rather than passive prompt inclusion; bootstrap runs load final skills, sub-agents, and memory of a successful from-scratch run before any new play | C.2 Reset-Free Bootstrap Transfer
M-C5-005 | SFT (LoRA) hyperparameters | Gemma-4 variants (E2B, E4B, 26B MoE, 31B dense) fine-tuned via LoRA r=256, alpha=256, bf16 precision, 8K-token context, Unsloth on H200 GPUs; LR 2e-5, linear warmup over 3% of training, cosine decay, one pass over teacher-trajectory set; each example is a (screenshot, harness prompt, teacher response) tuple extracted from Gemini-3.1-pro Continual Harness gameplay | D.1 Training Hyperparameters
M-C5-006 | offline GRPO | per state in teacher-visited set, SFT-initialized policy generates G=4 candidate completions; each scored independently by Gemini-3-flash-preview per-step oracle on composite of action correctness (binary, weight 0.6) and format compliance (binary, weight 0.4); advantages group-normalized within the G samples per state; LR 1e-6, KL coefficient beta=0.04 against SFT reference, batch size 8 states per optimizer step, 590 total optimization steps | D.1 Training Hyperparameters
M-C5-007 | online co-learning loop (DAgger+PRM) | each online iteration is a K=256-step DAgger rollout through full Continual Harness (memory, skills, sub-agents, prompt evolving) on Pokemon Red; pairwise process reward model (Gemini-3-flash-preview) scores each transition over a sliding window; reward = trajectory progress (0.4) + action correctness (0.3) + reasoning quality (0.2) + format compliance (0.1); low-reward windows relabeled by Gemini-3.1-pro teacher, soft SFT update on relabeled shard produces next checkpoint | D.1 Training Hyperparameters
Q-C5-001 | C.1.2 skill refinement triage | "The refinement loop therefore triages: it repairs the skills the agent depends on, tolerates regressions on unused ones, and accepts a long create-and-forget tail."
Q-C5-002 | C.1.3 sub-agent token partitioning | "the sub-agent curve sits about an order of magnitude below the orchestrator curve throughout, which is the per-step saving the harness buys by partitioning context."
Q-C5-003 | C.1.4 memory reference rate | "The reference rate remains low in absolute terms, which we report honestly; most authored entries sit unused."
Q-C5-004 | C.2 transferable unit | "The harness rather than the episode is the transferable unit: reset-free refinement carries across runs when the inherited components stay in play, and it breaks when the agent abandons them."
Q-C5-005 | D.2 smaller-size collapse | "Smaller sizes converge to low SFT training loss but collapse to tool_format=0 on the real harness prompt, consistent with an interaction between SFT signal strength and the 8K context needed to hold the full state plus reasoning."
B-C5-001 | sub-agent context partitioning saving | sub-agent token curve sits about an order of magnitude below orchestrator curve throughout the run (Figure 17); bootstrap-updating handoff count tracks from-scratch closely with small plateau gap by run end | C.1.3 Sub-agent handoffs
B-C5-002 | C5 inheritance shares (Table 2, n=3 seeds per cell) | Emerald: skills 100.0%+/-0.0 frozen / 99.6%+/-0.6 continued; subagents 100.0/100.0; memories 98.2%+/-1.9/100.0. Red: skills 100.0%+/-0.0 frozen / 96.5%+/-3.8 continued; subagents 100.0/6.4%+/-5.7; memories 100.0/100.0 | C.2 Reset-Free Bootstrap Transfer
B-C5-003 | Red bootstrap regression onset | regression begins around step 213, when newly authored sub-agents overtake the inherited ones | C.2.1 Red Bootstrap-Updating Regression
B-C5-004 | Gemma-4 Emerald eval matrix (Table 3) | Base 26B tool_format 0.00, actionable 0.20; SFT(emerald) 26B tool_format 0.95, actionable 0.95, grounding 0.72, hallucination 0.55; all GRPO-column cells degenerate; e2b/e4b SFT rows collapse to tool_format 0.00; tokens/s Base 26B 154 / SFT 26B 18 / GRPO 26B 26 | D.2 Gemma-4 Full Eval Matrix
B-C5-005 | Gemma-4 Red eval matrix (Table 4) | 26B Red SFT row omitted (adapter degenerate at eval time); 31B SFT Red: tool_format 0.50, actionable 0.50, grounding 0.44, action_relevance 0.75, reasoning_similarity 0.65, hallucination 0.30; GRPO 31B tokens/s 101 | D.2 Gemma-4 Full Eval Matrix
B-C5-006 | warm-up stage progression on Red (Table 5, 20 held-out transitions) | Base Gemma-4 26B tool_fmt 0.05, act'ble 0.25, act_rel 0.33, reason 0.25, ground 0.23; SFT Gemma-4 31B tool_fmt 0.50, act'ble 0.50, act_rel 0.75, reason 0.65, ground 0.44; Offline GRPO (heuristic) 26B tool_fmt 0.50, act_rel 0.65; Offline GRPO (Gemini oracle) 26B tool_fmt 0.50, act_rel 0.55; Qwen3.5 35B parseable tool calls but 0 game progress (stuck) | D.2 Gemma-4 Full Eval Matrix
C-C5-001 | harness transfer is conditional | harness-as-transferable-unit holds only when agent continues exercising inherited components; Red bootstrap-updating agents collapse sub-agent budget to a handful of calls and cite IDs not present in bootstrap, and milestone staircase regresses below H_min in lockstep | C.2 Reset-Free Bootstrap Transfer
C-C5-002 | un-repaired bootstrap sub-agents | newly authored sub-agents overtaking inherited ones at step 213 have not gone through the from-scratch skill repair cycle, so per-invocation success sits below what bootstrap sub-agents reached before caching; reuse prior on sub-agent selection or deletion rule deprecating newly authored sub-agents needed | C.2.1 Red Bootstrap-Updating Regression
C-C5-003 | 8K context interacts with model size | interaction between SFT signal strength and 8K-token context needed to hold full state plus reasoning causes smaller Gemma-4 sizes to collapse to tool_format=0 despite low SFT loss | D.2 Gemma-4 Full Eval Matrix
C-C5-004 | 31B SFT Emerald worse than 26B | the 31B SFT Emerald model underperforms the 26B SFT Emerald model on most metrics in our runs | D.2 Gemma-4 Full Eval Matrix
C-C5-005 | offline-GRPO checkpoint degenerate | the offline-GRPO warm-up checkpoint emits degenerate completions on the Emerald prompt set | D.2 Gemma-4 Full Eval Matrix
C-C5-006 | Qwen3.5 35B baseline cannot advance | Qwen3.5 35B produces parseable tool calls through the harness but cannot advance in the game | D.2 Gemma-4 Full Eval Matrix